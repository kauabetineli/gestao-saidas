spring:
  application:
    name: app
  datasource:
    password: 1234
    url: jdbc:mysql://localhost:3306/db_cont_saida
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
server:
  port: 8081